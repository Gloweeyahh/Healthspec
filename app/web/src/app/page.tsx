export default function Home() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center px-6">
      <div className="max-w-xl text-center">
        <h1 className="text-3xl font-semibold tracking-tight">HealthSpec</h1>
        <p className="mt-3 text-neutral-600">
          Know whether your health API actually conforms. This is the
          scaffold build — authentication and the workspace shell come next.
        </p>
      </div>
    </main>
  );
}
